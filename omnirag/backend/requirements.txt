# --- Phase 1: core web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
pytest-asyncio==0.25.2
reportlab==4.2.5   # test-only: generates real PDFs with actual text content, to test extraction against

# --- Phase 2: database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Phase 3: authentication ---
bcrypt==4.2.1
pyjwt==2.10.1
python-multipart==0.0.20  # required by FastAPI's OAuth2PasswordRequestForm (login form parsing)
email-validator==2.2.0    # required by pydantic's EmailStr

# --- Phase 4: document ingestion ---
celery==5.4.0
redis==5.2.1
pypdf==5.1.0
python-docx==1.1.2

# --- Phase 5: chunking + embeddings ---
openai==1.58.1  # OpenAIEmbeddingProvider — real implementation, needs OPENAI_API_KEY + network access

# --- Phase 6: vector database ---
qdrant-client==1.12.1

# --- Phase 7: basic RAG ---
anthropic==0.42.0  # AnthropicLLMProvider — real implementation, needs ANTHROPIC_API_KEY + network access

# NOTE: google-generativeai is intentionally NOT listed yet — no Gemini
# provider has been built, so it's not a real dependency of this codebase yet.
