# --- Phase 1: core web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
pytest-asyncio==0.25.2

# --- Phase 2: database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Phase 3: authentication ---
bcrypt==4.2.1
pyjwt==2.10.1
python-multipart==0.0.20  # required by FastAPI's OAuth2PasswordRequestForm (login form parsing)
email-validator==2.2.0    # required by pydantic's EmailStr

# NOTE: qdrant-client, celery, redis, openai, anthropic, google-generativeai
# etc. are intentionally NOT listed yet. They get added in the phase that
# actually introduces them (Phase 4 workers, Phase 6 vector DB, Phase 7 LLM
# providers) so requirements.txt always reflects what the code at that point
# in history actually uses.
